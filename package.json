{
  "name": "en-meg-sosem",
  "private": true,
  "version": "1.0.4",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:android": "vite build --mode android",
    "android:sync": "npm run build:android && cap sync android",
    "android:check": "npm run test && npm run validate:data && npm run android:sync && cd android && gradlew.bat lintDebug testDebugUnitTest assembleDebug",
    "android:release": "npm run android:sync && cd android && gradlew.bat lintDebug testDebugUnitTest assembleRelease",
    "android:open": "cap open android",
    "test": "node --test scripts/*.test.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "cloudflare:deploy": "wrangler deploy --cwd cloudflare/feedback-worker",
    "cloudflare:migrate": "wrangler d1 migrations apply jatek --remote --cwd cloudflare/feedback-worker"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/app": "8.1.1",
    "@capacitor/core": "8.5.0",
    "@capacitor/screen-orientation": "8.0.1",
    "lucide-react": "1.28.0",
    "peerjs": "^1.5.5",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0",
    "@vitejs/plugin-react": "6.0.5",
    "autoprefixer": "10.5.4",
    "postcss": "8.5.25",
    "tailwindcss": "4.3.3",
    "vite": "8.2.0",
    "wrangler": "^4.118.0"
  }
}
